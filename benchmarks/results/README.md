# Benchmark Results

`baseline-v0.1.0.json` is the committed reference the CI gate compares against.
Regenerate the baseline deliberately (and review it) when a real, intended
performance change lands.
