apiVersion: v1
kind: Service
metadata:
  name: meridian-controller
  namespace: meridian
  labels:
    app.kubernetes.io/component: controller
spec:
  selector:
    app.kubernetes.io/component: controller
  ports:
    - name: https
      port: 8443
      targetPort: 8443
