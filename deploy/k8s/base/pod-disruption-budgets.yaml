apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: meridian-controller
  namespace: meridian
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: controller
